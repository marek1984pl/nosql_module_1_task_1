spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/ticket_booking_test
    username: postgres
    password: a123456
    driver-class-name: org.postgresql.Driver
